<%= simple_form_for([@pin, @pin.comments.build]) do |f| %>
    <%= f.input :comment, lable: "Reply to thread" %>
    <%= f.button :submit, lable: "Add new comment", class: "btn btn-primary"%>
<% end %>